{
  "start": "echo 'Copying config file to target folder...' && cp config.txt ./target && echo 'Starting the compiled jar file...' && cd target && java -jar thunder-0.1.1-All.jar",
  "install": "mvn package",
  "watch": {
    "throttle": 900000000,
    "install": {
      "include": ["^pom\\.xml$", "\\.java$"]
    }
  }
}